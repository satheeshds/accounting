services:
  accounting:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - accounting_data:/data
    environment:
      - DB_PATH=/data/accounting.db
      - AUTH_USER=admin
      - AUTH_PASS=changeme
      - LOG_LEVEL=info
    restart: unless-stopped

volumes:
  accounting_data:
